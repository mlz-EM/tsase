"""Backward-compatible import shim for SSNEB filter adapters."""

from tsase.neb.constraints.adapters import (
    CellFilterAdapter,
    ImageFilterAdapter,
    SubsetFilterAdapter,
    make_filter_adapter,
)

